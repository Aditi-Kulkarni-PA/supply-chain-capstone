[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    ragas: RAGAS quality metrics — calls OpenAI internally, excluded from default runs.
           Run explicitly with: uv run pytest evals/ -m ragas -v
